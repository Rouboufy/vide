--- main.zig
+++ main.zig
@@ -37,10 +37,6 @@
     return try rpc.processNotifications();
 }
 
-fn logTrace(comptime fmt: []const u8, args: anytype) void {
-    std.debug.print(fmt, args);
-}
-
 fn openFile(rpc: *RpcClient, allocator: std.mem.Allocator, path: []const u8) !void {
-    logTrace("openFile called for {s}\n", .{path});
+    // Use nvim_input to feed the :edit command as keystrokes instead of
     // synchronous nvim_command which blocks the event loop waiting for response
@@ -51,6 +47,3 @@
     input_params[0] = .{ .string = cmd };
-    logTrace("openFile calling rpc.call...\n", .{});
     const result = try rpc.call("nvim_input", input_params);
-    logTrace("openFile rpc.call returned\n", .{});
     msgpack.freeValue(result, allocator);
-    logTrace("openFile done\n", .{});
